import * as actionTypes from "../actions/actionTypes";

const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    // case actionTypes.ddd:
    //   return {
    //     ...state
    //
    //   };

    default:
      return state;
  }
};

export default reducer;
